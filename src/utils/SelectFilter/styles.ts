import styled from 'styled-components';

export const Box = styled.div`
  display: flex;

  p {
    margin: auto 0;
    margin-right: 1rem;
  }
  select {
    padding: 10px;
    border-radius: 0.7rem;
    margin-right: 2rem;
    border: none;
    outline: 0px;
    font-size: 15px;
  }
`;
